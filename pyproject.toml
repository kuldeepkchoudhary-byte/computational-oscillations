[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coupled_oscillators"
version = "0.1.0"
description = "Coupled Oscillators and Magnonics Numerical Project"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "jupyter"
]
